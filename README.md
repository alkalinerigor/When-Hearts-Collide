# When-Hearts-Collide
This lab demonstrates the insecurity of the MD5 algorithm. By generating a hash collision using fastcoll, I created a unique file that shares an identical hash with a target image. The server’s integrity check was bypassed, proving that MD5 is obsolete for security and must be replaced by SHA-256.
